<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/MarchingCubesMesher (snapshot).cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/MarchingCubesMesher (snapshot).cs" />
              <option name="originalContent" value="using System;&#10;using _Project.World.Planet.Scripts.MarchingCubes.Core;&#10;using _Project.World.Planet.Scripts.MarchingCubes.DensitySampling;&#10;using Unity.Mathematics;&#10;&#10;namespace _Project.World.Planet.Scripts.MarchingCubes.MeshGeneration&#10;{&#10;    public class MarchingCubesMesher&#10;    {&#10;        private const float IsoLevel = 0.5f;&#10;        &#10;        &#10;        public static void GenerateAt(int3 pos, DensityField grid, MeshDataBuilder meshDataBuilder)&#10;        {&#10;            int cubeIndex = GetCubeIndexAt(pos, grid, IsoLevel); // calculates the cube index at that position. look at the description of that function if you want to know what it does&#10;            &#10;            int edgeFlags = McTables.EdgeTable[cubeIndex]; // converts the given corner configuration to a binary number where every bit represents one edge. note that we convert an 8 bit number (one bit for every corner) to a 12 bit number (one bit for every edge). &#10;            // we take that value from a huge precomputed table that contains the edge configuration for every possible corner configuration. So every bit is 1 if the isosurface cuts through that edge. &#10;&#10;&#10;            if (edgeFlags == 0) // if the isosurface doesnt cut through any edge, we can skip this cube and return an empty list of triangles&#10;                return;&#10;&#10;            float3[] vertices = new float3[12]; // the resulting vertices can be 12 max since there are only 12 edges. we fill the rest of the values with -1.&#10;            &#10;            // helper vars for the single corners of the cube&#10;            int3 p0 = new int3(pos.x, pos.y, pos.z);&#10;            int3 p1 = new int3(pos.x + 1, pos.y, pos.z);&#10;            int3 p2 = new int3(pos.x + 1, pos.y+1, pos.z);&#10;            int3 p3 = new int3(pos.x, pos.y+1, pos.z);&#10;            int3 p4 = new int3(pos.x, pos.y, pos.z+1);&#10;            int3 p5 = new int3(pos.x + 1, pos.y, pos.z+1);&#10;            int3 p6 = new int3(pos.x + 1, pos.y + 1, pos.z + 1);&#10;            int3 p7 = new int3(pos.x, pos.y + 1, pos.z + 1);&#10;            &#10;            &#10;            // here we check if the given bit is 1. the binary operator &amp; acts as an AND mask, so 0b11011101 &amp; 0b01010011 = 0b01010001. this way we can check every edge separately. c# doesnt let you directly cast ints to bools (1=true, 0=false) so we just compare it to 0 to know if its true or not&#10;            if ((edgeFlags &amp; 1) != 0)&#10;                vertices[0] = // so we know now that this edge is used but if we just pass the center between the start and end point of that edge it would be way to blocky. so we just interpolate between the 2 densities of the corresponding values. this way everything is distributed smoothly.&#10;                    VertexInterp(IsoLevel, p0, p1, grid.DensityAt(p0), grid.DensityAt(p1));&#10;            if ((edgeFlags &amp; 2) != 0)&#10;                vertices[1] =&#10;                    VertexInterp(IsoLevel, p1, p2, grid.DensityAt(p1), grid.DensityAt(p2));&#10;            if ((edgeFlags &amp; 4) != 0)&#10;                vertices[2] =&#10;                    VertexInterp(IsoLevel, p2, p3, grid.DensityAt(p2), grid.DensityAt(p3));&#10;            if ((edgeFlags &amp; 8) != 0)&#10;                vertices[3] =&#10;                    VertexInterp(IsoLevel, p3, p0, grid.DensityAt(p3), grid.DensityAt(p0));&#10;            if ((edgeFlags &amp; 16) != 0)&#10;                vertices[4] =&#10;                    VertexInterp(IsoLevel, p4, p5, grid.DensityAt(p4), grid.DensityAt(p5));&#10;            if ((edgeFlags &amp; 32) != 0)&#10;                vertices[5] =&#10;                    VertexInterp(IsoLevel, p5, p6, grid.DensityAt(p5), grid.DensityAt(p6));&#10;            if ((edgeFlags &amp; 64) != 0)&#10;                vertices[6] =&#10;                    VertexInterp(IsoLevel, p6, p7, grid.DensityAt(p6), grid.DensityAt(p7));&#10;            if ((edgeFlags &amp; 128) != 0)&#10;                vertices[7] =&#10;                    VertexInterp(IsoLevel, p7, p4, grid.DensityAt(p7), grid.DensityAt(p4));&#10;            if ((edgeFlags &amp; 256) != 0)&#10;                vertices[8] =&#10;                    VertexInterp(IsoLevel, p0, p4, grid.DensityAt(p0), grid.DensityAt(p4));&#10;            if ((edgeFlags &amp; 512) != 0)&#10;                vertices[9] =&#10;                    VertexInterp(IsoLevel, p1, p5, grid.DensityAt(p1), grid.DensityAt(p5));&#10;            if ((edgeFlags &amp; 1024) != 0)&#10;                vertices[10] =&#10;                    VertexInterp(IsoLevel, p2, p6, grid.DensityAt(p2), grid.DensityAt(p6));&#10;            if ((edgeFlags &amp; 2048) != 0)&#10;                vertices[11] =&#10;                    VertexInterp(IsoLevel, p3, p7, grid.DensityAt(p3), grid.DensityAt(p7));&#10;            &#10;            &#10;            //now we need to convert the vertices to triangles and take just the filled ones&#10;            &#10;            //so we have the used edges but we dont know how the triangles are supposed to be generated to occupy the given edge configuration. &#10;            // thats why we have another huge precomputed table that contains the triangle configuration for every possible edge configuration. so we just need to loop through that table until we find a -1 which marks the end of the triangle list for that edge configuration.&#10;            for (int i=0;McTables.TriTable[cubeIndex, i]!=-1;i+=3) { // go in steps of 3 (a triangle consists of 3 points ) until we find a -1 which marks the end of that triangle list&#10;                meshDataBuilder.AddTriangle(&#10;                    vertices[McTables.TriTable[cubeIndex, i]],&#10;                    vertices[McTables.TriTable[cubeIndex, i+1]],&#10;                    vertices[McTables.TriTable[cubeIndex, i+2]]&#10;                );&#10;            }&#10;        }&#10;&#10;&#10;        // returns the cube index of a given position in a given grid using the given iso level as a threshold. the cube index is an 8 bit integer where every bit represents whether that corner is inside (1) or outside (0) of the isosurface. &#10;        private static int GetCubeIndexAt(int3 pos, DensityField grid, float isoLevel)&#10;        {&#10;            int cubeIndex = 0;&#10;&#10;            if (grid.DensityAt(pos.x, pos.y, pos.z) &gt; isoLevel) cubeIndex |= 1;// the |= operator sets every bit that is set in the right operand to 1 in the left operand also to 1.&#10;                                                                               // so if the left number (cubeIndex) is 0b11001000 and the right one (the mask, lets say 4) is 0b00000100 the result of that operation would be 0b11001100&#10;            if (grid.DensityAt(pos.x + 1, pos.y, pos.z) &gt; isoLevel) cubeIndex |= 2;&#10;            if (grid.DensityAt(pos.x + 1, pos.y+1, pos.z) &gt; isoLevel) cubeIndex |= 4;&#10;            if (grid.DensityAt(pos.x, pos.y+1, pos.z) &gt; isoLevel) cubeIndex |= 8;&#10;            if (grid.DensityAt(pos.x, pos.y, pos.z+1) &gt; isoLevel) cubeIndex |= 16;&#10;            if (grid.DensityAt(pos.x + 1, pos.y, pos.z+1) &gt; isoLevel) cubeIndex |= 32;&#10;            if (grid.DensityAt(pos.x + 1, pos.y + 1, pos.z + 1) &gt; isoLevel) cubeIndex |= 64;&#10;            if (grid.DensityAt(pos.x, pos.y + 1, pos.z + 1) &gt; isoLevel) cubeIndex |= 128;&#10;&#10;            return cubeIndex;&#10;        }&#10;&#10;&#10;        // lerps (linear interpolates) between 2 given points based on their density values and the iso level&#10;        private static float3 VertexInterp(float isoLevel, int3 p1, int3 p2, float valP1, float valP2)&#10;        {&#10;            float3 p;&#10;&#10;            if (Math.Abs(isoLevel - valP1) &lt; 0.00001)&#10;                return (new float3(p1.x, p1.y, p1.z));&#10;            if (Math.Abs(isoLevel - valP2) &lt; 0.00001)&#10;                return (new float3(p2.x, p2.y, p2.z));&#10;            if (Math.Abs(valP1 - valP2) &lt; 0.00001)&#10;                return (new float3(p1.x, p1.y, p1.z));&#10;            &#10;            float mu = (isoLevel - valP1) / (valP2 - valP1);&#10;            p.x = p1.x + mu * (p2.x - p1.x);&#10;            p.y = p1.y + mu * (p2.y - p1.y);&#10;            p.z = p1.z + mu * (p2.z - p1.z);&#10;&#10;            return p;&#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="using System;&#10;using _Project.World.Planet.Scripts.MarchingCubes.Core;&#10;using _Project.World.Planet.Scripts.MarchingCubes.DensitySampling;&#10;using Unity.Mathematics;&#10;&#10;namespace _Project.World.Planet.Scripts.MarchingCubes.MeshGeneration&#10;{&#10;    public class MarchingCubesMesher&#10;    {&#10;        private const float IsoLevel = 0.5f;&#10;        private const float IsoEpsilon = 1e-6f;&#10;        &#10;        &#10;        public static void GenerateAt(int3 pos, DensityField grid, MeshDataBuilder meshDataBuilder)&#10;        {&#10;            int cubeIndex = GetCubeIndexAt(pos, grid, IsoLevel); // calculates the cube index at that position. look at the description of that function if you want to know what it does&#10;            &#10;            int edgeFlags = McTables.EdgeTable[cubeIndex]; // converts the given corner configuration to a binary number where every bit represents one edge. note that we convert an 8 bit number (one bit for every corner) to a 12 bit number (one bit for every edge). &#10;            // we take that value from a huge precomputed table that contains the edge configuration for every possible corner configuration. So every bit is 1 if the isosurface cuts through that edge. &#10;&#10;&#10;            if (edgeFlags == 0) // if the isosurface doesnt cut through any edge, we can skip this cube and return an empty list of triangles&#10;                return;&#10;&#10;            float3[] vertices = new float3[12]; // the resulting vertices can be 12 max since there are only 12 edges. we fill the rest of the values with -1.&#10;            &#10;            // helper vars for the single corners of the cube&#10;            int3 p0 = new int3(pos.x, pos.y, pos.z);&#10;            int3 p1 = new int3(pos.x + 1, pos.y, pos.z);&#10;            int3 p2 = new int3(pos.x + 1, pos.y+1, pos.z);&#10;            int3 p3 = new int3(pos.x, pos.y+1, pos.z);&#10;            int3 p4 = new int3(pos.x, pos.y, pos.z+1);&#10;            int3 p5 = new int3(pos.x + 1, pos.y, pos.z+1);&#10;            int3 p6 = new int3(pos.x + 1, pos.y + 1, pos.z + 1);&#10;            int3 p7 = new int3(pos.x, pos.y + 1, pos.z + 1);&#10;            &#10;            &#10;            // here we check if the given bit is 1. the binary operator &amp; acts as an AND mask, so 0b11011101 &amp; 0b01010011 = 0b01010001. this way we can check every edge separately. c# doesnt let you directly cast ints to bools (1=true, 0=false) so we just compare it to 0 to know if its true or not&#10;            if ((edgeFlags &amp; 1) != 0)&#10;                vertices[0] = // so we know now that this edge is used but if we just pass the center between the start and end point of that edge it would be way to blocky. so we just interpolate between the 2 densities of the corresponding values. this way everything is distributed smoothly.&#10;                    VertexInterp(IsoLevel, p0, p1, grid.DensityAt(p0), grid.DensityAt(p1));&#10;            if ((edgeFlags &amp; 2) != 0)&#10;                vertices[1] =&#10;                    VertexInterp(IsoLevel, p1, p2, grid.DensityAt(p1), grid.DensityAt(p2));&#10;            if ((edgeFlags &amp; 4) != 0)&#10;                vertices[2] =&#10;                    VertexInterp(IsoLevel, p2, p3, grid.DensityAt(p2), grid.DensityAt(p3));&#10;            if ((edgeFlags &amp; 8) != 0)&#10;                vertices[3] =&#10;                    VertexInterp(IsoLevel, p3, p0, grid.DensityAt(p3), grid.DensityAt(p0));&#10;            if ((edgeFlags &amp; 16) != 0)&#10;                vertices[4] =&#10;                    VertexInterp(IsoLevel, p4, p5, grid.DensityAt(p4), grid.DensityAt(p5));&#10;            if ((edgeFlags &amp; 32) != 0)&#10;                vertices[5] =&#10;                    VertexInterp(IsoLevel, p5, p6, grid.DensityAt(p5), grid.DensityAt(p6));&#10;            if ((edgeFlags &amp; 64) != 0)&#10;                vertices[6] =&#10;                    VertexInterp(IsoLevel, p6, p7, grid.DensityAt(p6), grid.DensityAt(p7));&#10;            if ((edgeFlags &amp; 128) != 0)&#10;                vertices[7] =&#10;                    VertexInterp(IsoLevel, p7, p4, grid.DensityAt(p7), grid.DensityAt(p4));&#10;            if ((edgeFlags &amp; 256) != 0)&#10;                vertices[8] =&#10;                    VertexInterp(IsoLevel, p0, p4, grid.DensityAt(p0), grid.DensityAt(p4));&#10;            if ((edgeFlags &amp; 512) != 0)&#10;                vertices[9] =&#10;                    VertexInterp(IsoLevel, p1, p5, grid.DensityAt(p1), grid.DensityAt(p5));&#10;            if ((edgeFlags &amp; 1024) != 0)&#10;                vertices[10] =&#10;                    VertexInterp(IsoLevel, p2, p6, grid.DensityAt(p2), grid.DensityAt(p6));&#10;            if ((edgeFlags &amp; 2048) != 0)&#10;                vertices[11] =&#10;                    VertexInterp(IsoLevel, p3, p7, grid.DensityAt(p3), grid.DensityAt(p7));&#10;            &#10;            &#10;            //now we need to convert the vertices to triangles and take just the filled ones&#10;            &#10;            //so we have the used edges but we dont know how the triangles are supposed to be generated to occupy the given edge configuration. &#10;            // thats why we have another huge precomputed table that contains the triangle configuration for every possible edge configuration. so we just need to loop through that table until we find a -1 which marks the end of the triangle list for that edge configuration.&#10;            for (int i=0;McTables.TriTable[cubeIndex, i]!=-1;i+=3) { // go in steps of 3 (a triangle consists of 3 points ) until we find a -1 which marks the end of that triangle list&#10;                meshDataBuilder.AddTriangle(&#10;                    vertices[McTables.TriTable[cubeIndex, i]],&#10;                    vertices[McTables.TriTable[cubeIndex, i+1]],&#10;                    vertices[McTables.TriTable[cubeIndex, i+2]]&#10;                );&#10;            }&#10;        }&#10;&#10;&#10;        // returns the cube index of a given position in a given grid using the given iso level as a threshold. the cube index is an 8 bit integer where every bit represents whether that corner is inside (1) or outside (0) of the isosurface. &#10;        private static int GetCubeIndexAt(int3 pos, DensityField grid, float isoLevel)&#10;        {&#10;            int cubeIndex = 0;&#10;&#10;            bool IsInside(float d) =&gt; d &gt; (isoLevel + IsoEpsilon);&#10;&#10;            if (IsInside(grid.DensityAt(pos.x, pos.y, pos.z))) cubeIndex |= 1;// the |= operator sets every bit that is set in the right operand to 1 in the left operand also to 1.&#10;                                                                               // so if the left number (cubeIndex) is 0b11001000 and the right one (the mask, lets say 4) is 0b00000100 the result of that operation would be 0b11001100&#10;            if (IsInside(grid.DensityAt(pos.x + 1, pos.y, pos.z))) cubeIndex |= 2;&#10;            if (IsInside(grid.DensityAt(pos.x + 1, pos.y+1, pos.z))) cubeIndex |= 4;&#10;            if (IsInside(grid.DensityAt(pos.x, pos.y+1, pos.z))) cubeIndex |= 8;&#10;            if (IsInside(grid.DensityAt(pos.x, pos.y, pos.z+1))) cubeIndex |= 16;&#10;            if (IsInside(grid.DensityAt(pos.x + 1, pos.y, pos.z+1))) cubeIndex |= 32;&#10;            if (IsInside(grid.DensityAt(pos.x + 1, pos.y + 1, pos.z + 1))) cubeIndex |= 64;&#10;            if (IsInside(grid.DensityAt(pos.x, pos.y + 1, pos.z + 1))) cubeIndex |= 128;&#10;&#10;            return cubeIndex;&#10;        }&#10;&#10;&#10;        // lerps (linear interpolates) between 2 given points based on their density values and the iso level&#10;        private static float3 VertexInterp(float isoLevel, int3 p1, int3 p2, float valP1, float valP2)&#10;        {&#10;            float3 p;&#10;&#10;            if (Math.Abs(isoLevel - valP1) &lt; IsoEpsilon &amp;&amp; Math.Abs(isoLevel - valP2) &lt; IsoEpsilon)&#10;                return (new float3(p1.x + p2.x, p1.y + p2.y, p1.z + p2.z)) * 0.5f;&#10;            if (Math.Abs(isoLevel - valP1) &lt; IsoEpsilon)&#10;                return (new float3(p1.x, p1.y, p1.z));&#10;            if (Math.Abs(isoLevel - valP2) &lt; IsoEpsilon)&#10;                return (new float3(p2.x, p2.y, p2.z));&#10;            if (Math.Abs(valP1 - valP2) &lt; IsoEpsilon)&#10;                return (new float3(p1.x + p2.x, p1.y + p2.y, p1.z + p2.z)) * 0.5f;&#10;&#10;            float mu = (isoLevel - valP1) / (valP2 - valP1);&#10;            p.x = p1.x + mu * (p2.x - p1.x);&#10;            p.y = p1.y + mu * (p2.y - p1.y);&#10;            p.z = p1.z + mu * (p2.z - p1.z);&#10;&#10;            return p;&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/MeshDataBuilder (snapshot).cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/MeshDataBuilder (snapshot).cs" />
              <option name="originalContent" value="using System.Collections.Generic;&#10;using Unity.Mathematics;&#10;&#10;namespace _Project.World.Planet.Scripts.MarchingCubes.MeshGeneration&#10;{&#10;    public class MeshDataBuilder&#10;    {&#10;        private readonly MeshData _meshData = new();&#10;&#10;        private readonly Dictionary&lt;float3, int&gt; _vertexMap = new(); // this is used for index deduplication.&#10;                                                                     // since every triangle has 3 vertices and many triangles share vertices with each other, we want to avoid adding the same vertex multiple times to the vertices list.&#10;                                                                     // that would be a waste of memory and also cause visual artifacts. so we use this dictionary to check if we already added a vertex and if so we just reuse its index instead of adding it again.&#10;&#10;&#10;        public MeshData Build() =&gt; _meshData;&#10;&#10;        /// &lt;summary&gt;&#10;        /// this adds a triangle to the mesh and calculates its indices and normals. this way you can just call this method for every triangle you want to add and it will take care of the rest.&#10;        /// &lt;/summary&gt;&#10;        /// &lt;param name=&quot;a-c&quot;&gt;the 3 corners of the triangle&lt;/param&gt;&#10;        public void AddTriangle(float3 a, float3 b, float3 c)&#10;        {&#10;            int i0 = GetOrAddVertex(a);&#10;            int i1 = GetOrAddVertex(b);&#10;            int i2 = GetOrAddVertex(c);&#10;&#10;            _meshData.Indices.Add(i0);&#10;            _meshData.Indices.Add(i1);&#10;            _meshData.Indices.Add(i2);&#10;&#10;            float3 normal = math.normalize(math.cross(b - a, c - a)); // this calculates a good normal value based on the triangle vertices, which is important for the lighting to look smooth.&#10;&#10;            _meshData.Normals[i0] += normal;&#10;            _meshData.Normals[i1] += normal;&#10;            _meshData.Normals[i2] += normal;&#10;        }&#10;        &#10;        /// &lt;summary&gt;&#10;        /// checks if the given vertex already exists in the vertices list and if so returns its index,&#10;        /// otherwise it adds it to the list and returns the new index&#10;        /// &lt;/summary&gt;&#10;        /// &lt;param name=&quot;v&quot;&gt;the vertex to add or get&lt;/param&gt;&#10;        private int GetOrAddVertex(float3 v)&#10;        {&#10;            if (_vertexMap.TryGetValue(v, out int index))&#10;                return index;&#10;&#10;            index = _meshData.Vertices.Count;&#10;&#10;            _meshData.Vertices.Add(v);&#10;            _meshData.Normals.Add(float3.zero);&#10;&#10;            _vertexMap.Add(v, index);&#10;&#10;            return index;&#10;        }&#10;&#10;        /// &lt;summary&gt;&#10;        /// this normalizes the normals. that means every normal has a length of exactly 1.&#10;        /// &lt;/summary&gt;&#10;        public void NormalizeNormals()&#10;        {&#10;            for (int i = 0; i &lt; _meshData.Normals.Count; i++)&#10;            {&#10;                _meshData.Normals[i] =&#10;                    math.normalize(_meshData.Normals[i]);&#10;            }&#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="using System.Collections.Generic;&#10;using Unity.Mathematics;&#10;&#10;namespace _Project.World.Planet.Scripts.MarchingCubes.MeshGeneration&#10;{&#10;    public class MeshDataBuilder&#10;    {&#10;        private readonly MeshData _meshData = new();&#10;&#10;        private readonly Dictionary&lt;float3, int&gt; _vertexMap = new(); // this is used for index deduplication.&#10;                                                                     // since every triangle has 3 vertices and many triangles share vertices with each other, we want to avoid adding the same vertex multiple times to the vertices list.&#10;                                                                     // that would be a waste of memory and also cause visual artifacts. so we use this dictionary to check if we already added a vertex and if so we just reuse its index instead of adding it again.&#10;&#10;&#10;        public MeshData Build() =&gt; _meshData;&#10;&#10;        /// &lt;summary&gt;&#10;        /// this adds a triangle to the mesh and calculates its indices and normals. this way you can just call this method for every triangle you want to add and it will take care of the rest.&#10;        /// &lt;/summary&gt;&#10;        /// &lt;param name=&quot;a-c&quot;&gt;the 3 corners of the triangle&lt;/param&gt;&#10;        public void AddTriangle(float3 a, float3 b, float3 c)&#10;        {&#10;            // Skip triangles with invalid or degenerate data to avoid NaN normals.&#10;            if (!math.all(math.isfinite(a)) || !math.all(math.isfinite(b)) || !math.all(math.isfinite(c)))&#10;                return;&#10;&#10;            float3 ab = b - a;&#10;            float3 ac = c - a;&#10;            float3 normal = math.cross(ab, ac);&#10;            if (math.lengthsq(normal) &lt; 1e-12f)&#10;                return;&#10;&#10;            int i0 = GetOrAddVertex(a);&#10;            int i1 = GetOrAddVertex(b);&#10;            int i2 = GetOrAddVertex(c);&#10;&#10;            _meshData.Indices.Add(i0);&#10;            _meshData.Indices.Add(i1);&#10;            _meshData.Indices.Add(i2);&#10;&#10;            normal = math.normalize(normal);&#10;&#10;            _meshData.Normals[i0] += normal;&#10;            _meshData.Normals[i1] += normal;&#10;            _meshData.Normals[i2] += normal;&#10;        }&#10;        &#10;        /// &lt;summary&gt;&#10;        /// checks if the given vertex already exists in the vertices list and if so returns its index,&#10;        /// otherwise it adds it to the list and returns the new index&#10;        /// &lt;/summary&gt;&#10;        /// &lt;param name=&quot;v&quot;&gt;the vertex to add or get&lt;/param&gt;&#10;        private int GetOrAddVertex(float3 v)&#10;        {&#10;            if (_vertexMap.TryGetValue(v, out int index))&#10;                return index;&#10;&#10;            index = _meshData.Vertices.Count;&#10;&#10;            _meshData.Vertices.Add(v);&#10;            _meshData.Normals.Add(float3.zero);&#10;&#10;            _vertexMap.Add(v, index);&#10;&#10;            return index;&#10;        }&#10;&#10;        /// &lt;summary&gt;&#10;        /// this normalizes the normals. that means every normal has a length of exactly 1.&#10;        /// &lt;/summary&gt;&#10;        public void NormalizeNormals()&#10;        {&#10;            for (int i = 0; i &lt; _meshData.Normals.Count; i++)&#10;            {&#10;                float3 n = _meshData.Normals[i];&#10;                if (!math.all(math.isfinite(n)) || math.lengthsq(n) &lt; 1e-12f)&#10;                {&#10;                    _meshData.Normals[i] = new float3(0f, 1f, 0f);&#10;                    continue;&#10;                }&#10;&#10;                _meshData.Normals[i] = math.normalize(n);&#10;            }&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>